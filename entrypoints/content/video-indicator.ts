import { getVideos, isVideoCollected } from '@/utils/storage';
import { normalizeUrl, sleep, getCurrentPageType } from './utils';

export async function initVideoIndicators() {
  console.log('视频标识功能已暂时禁用');
}

export async function refreshIndicators() {
  console.log('刷新标识功能已暂时禁用');
}
